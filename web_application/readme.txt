Run following commands to set up virtual env and then lastly run streamlit command:

pip install virtualenv
virtualenv newenv
source newenv/bin/activate or newenv\Scripts\activate
pip install -r requirements.txt
streamlit run app/app.py
